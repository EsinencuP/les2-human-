using System;

class Program
{
    static void Main()
    {
        Human[] Party = new Human[6]; //?  создание массива из 6 объектов типа Human, который будет использоваться для хранения участников вечеринки. Каждый элемент массива может быть экземпля
        for(int i = 0; i < Party.Length; i +=2) //? цикл for, который инициализирует элементы массива Party, созда
        {
        Party[i] = new Man(); //? инициализация каждого второго элемента массива Party объект
        Party[i+1] = new Woman(); //? инициализация каждого второго элемента массива Party объект
        }
        
        for (int i=0; i < Party.Length; i++ ) //? цикл for, который проходит по всем элементам массива Party и вызывает метод Print() для каждого участника вечеринки, чтобы вывести информацию о каждом участнике на консоль
        {
            Party[i].Print(); //? вызов метода Print() для каждого участника вечеринки, чтобы вывести информацию о каждом участнике на консоль
        }
        
        Man Bob = new Man(); //? создание объекта
        Human Siri = new Woman(); //? создание объекта типа Human, который на самом деле является
        Siri.Print(); //? вызов метода Print() для объекта Siri, который является
        ((Woman)Siri).makeUp(); //? приведение типа объекта Siri к типу 
    
            for (int i= 0; i < Party.Length; i++) //? цикл for, который проходит по всем элементам массива Party и вызывает методы SayHi() и beerDay() для каждого участника вечеринки, чтобы продемонстрировать их реакцию на празднование дня рождения и изменения в их состоянии после этого
                {
                    
                    Party[i].SayHi(); //? вызов метода SayHi() для каждого участника вечеринки, чтобы продемонстрировать их реакцию на празднование дня рождения. Этот метод может выводить различные сообщения в зависимости от типа участника (например, мужчина может говорить "Hey bro!", а женщина может говорить "Hi hi hi!" или "F*ck" в зависимости от уровня "битчности")
                    Party[i].beerDay(); //? вызов метода beerDay() для каждого участника вечеринки, чтобы продемонстрировать изменения в их состоянии после празднования дня рождения. Этот метод может изменять различные свойства участников (например, увеличивать возраст, изменять настроение, уменьшать уровень красоты и т.д.) и выводить соответствующие сообщения на консоль
                    Console.WriteLine("After Party"); //? вывод сообщения "After Party" на консоль после вызова методов SayHi() и beerDay() для каждого участника вечеринки, чтобы показать, что изменения в состоянии участников произошли после празднования дня рождения
                    Party[i].SayHi(); //? повторный вызов метода SayHi() для каждого участника вечеринки после вызова метода beerDay(), чтобы показать, как их реакция изменилась после празднования дня рождения. Например, мужчина может продолжать говорить "Hey bro!", а женщина может изменять свое приветствие в зависимости от изменений в ее состоянии (например, если уровень "битчности" уменьшился, она может начать говорить "Hi hi hi!" вместо "F*ck")
                }
            //
    }
}


